import React from "react";

const FilterBar = () => (
  <div className="flex items-center gap-2 mb-6">
    {/* Removed All, Stake, Vaults, Operate, Ecosystem buttons as requested */}
    {/* Search and TVL components removed as requested */}
  </div>
);

export default FilterBar;
